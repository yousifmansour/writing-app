services:
  mongo:
    image: mongo:8
    container_name: writing-app-mongo
    restart: unless-stopped
    ports:
      - "27018:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
